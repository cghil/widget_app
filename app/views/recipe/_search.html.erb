<div class="h10"></div>
<div class="text-center">
  <%= form_for recipe_search_path, url: {action: 'search', controller: 'recipe' }, class: "pure-form" do |f| %>
    <%= f.label :Find_recipe, class: "" %>
    <%= f.text_field :search, class: '' %>
    <%= f.submit "Search", class: 'pure-button pure-button-active button-large' %>
  <% end %>
</div>

<h3 class="text-center recipe-title">
  Click A Recipe
</h3>

<div class="recipes">
  <% @recipes= Recipe.get_recipe %>
    <% recipes = @recipes['recipes'] %>
    <% recipes.each do |recipe| %>
    <p class="text-center"> <%= recipe['title'] %> </p>
    <a href="<%= recipe['source_url']%>">
      <img class="margin-bottom margin-left resize thumbnail text-center" src="<%= recipe['image_url'] %>"/>
    </a>
  <% end %>
</div>
